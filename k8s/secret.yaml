# Substitua os valores em base64 antes de aplicar (kubectl create secret generic --from-literal=...)
apiVersion: v1
kind: Secret
metadata:
  name: agro-django-secret
  namespace: agro-saas
type: Opaque
stringData:
  DB_PASSWORD: "agro_pass"
  SECRET_KEY: "troque-isto-em-producao-chave-secreta-longa"
