apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: agro-saas
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
  clusterIP: None  # Headless para StatefulSet
